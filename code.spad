
-- Finds the square of an integer and the area of a circle.
-- For some reason these two need to be one procedure.
-- ASAT, I suppose :)
procedure Square(X : in Integer; XF : in Float;
	S : out Integer; SF : out Float) is
begin
	S := X * X;
	SF := XF * XF * 3.14159;
end Square;
